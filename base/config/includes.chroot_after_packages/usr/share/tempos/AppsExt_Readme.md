This folder contains portable, external applications that runs outside the .iso
This folder is mounted to /AppsExt on the system
Put Applications here in it's own folder. Folders with matching (application) names are updated, other folders are not deleted on update.
Put a .desktop file in each folder (only one level!) and a desktop icon is created